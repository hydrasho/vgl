image: "alpine:latest"
    
pages:
  stage: deploy
  script:
    - apk add vala
    - valadoc src/*.vala --pkg=sdl2 -o burgerdoc
    - mv burgerdoc public
    - ls -la
    - echo "Pages deployment job at ${CI_PAGES_URL}"
  artifacts:
    paths:
      - public